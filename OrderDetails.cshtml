@{
    ViewData["Title"] = "Danh Sách Đơn Hàng";
    Layout = "~/Areas/DeliveringStaff/Views/Shared/_Layout.cshtml";
}

<link rel="stylesheet" href="~/css/deliveringStaff.css" />

<div class="container">
    <div class="form-container">
        <h2 class="page-title">Danh Sách Đơn Hàng</h2>
        <div class="card">
            <div class="card-body">
                <h5>Thông Tin Đơn Hàng</h5>
                <p><strong>Tên Hàng:</strong> Đơn hàng 1</p>
            </div>
             <div class="text-right">
                <a href="/DeliveringStaff/Home/OrderDetails?orderId={{ order.id }}" class="btn btn-primary">Xem Chi Tiết</a>
            </div>
        </div>
    </div>
        <div class="card">
            <div class="card-body">
                <h5>Thông Tin Đơn Hàng</h5>
                <p><strong>Tên Hàng:</strong> Đơn hàng 2</p>
            </div>
             <div class="text-right">
                <a href="/DeliveringStaff/Home/OrderDetails?orderId={{ order.id }}" class="btn btn-primary">Xem Chi Tiết</a>
            </div>
        </div>
        <div class="card">
            <div class="card-body">
                <h5>Thông Tin Đơn Hàng</h5>
                <p><strong>Tên Hàng:</strong> Đơn hàng 3</p>
        </div>
         <div class="text-right">
                <a href="/DeliveringStaff/Home/OrderDetails?orderId={{ order.id }}" class="btn btn-primary">Xem Chi Tiết</a>
            </div>
    </div>

