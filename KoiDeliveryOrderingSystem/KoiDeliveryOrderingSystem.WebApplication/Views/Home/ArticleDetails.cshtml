@{
    ViewData["Title"] = ViewBag.Title;
}

<div class="article-details">
    <h1>@ViewBag.Title</h1>
    <img src="@ViewBag.ImageUrl" alt="@ViewBag.Title" class="img-fluid mb-3" />
    <p>@ViewBag.Content</p>
    <a href="/" class="btn btn-primary">Quay lại</a>
</div>
